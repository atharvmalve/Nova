import Link from "next/link";
import { Button } from "@/components/ui/button";
import { date, money } from "@/components/admin/customer-table";
import type { AdminCustomerDetail } from "@/services/admin-customers";
export function CustomerDetail({ customer }: { customer: AdminCustomerDetail }) { return <><header className="mt-5"><p className="text-xs font-semibold uppercase tracking-[.18em] text-muted-foreground">Customer</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{customer.name}</h1><p className="mt-2 text-sm text-muted-foreground">{customer.email}</p></header><div className="mt-7 grid gap-5 lg:grid-cols-[minmax(0,1.5fr)_18rem]"><section className="overflow-hidden rounded-xl border bg-background"><div className="border-b p-5"><h2 className="font-semibold">Order history</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead className="bg-muted/40 text-xs text-muted-foreground"><tr><th className="px-5 py-3">Order</th><th className="px-5 py-3">Amount</th><th className="px-5 py-3">Payment</th><th className="px-5 py-3">Fulfillment</th><th className="px-5 py-3">Date</th></tr></thead><tbody className="divide-y">{customer.orders.map((order) => <tr key={order.id}><td className="px-5 py-4 font-medium"><Link className="hover:underline" href={`/admin/orders/${order.id}`}>{order.orderNumber}</Link></td><td className="px-5 py-4">{money(order.totalPaise)}</td><td className="px-5 py-4 capitalize">{order.paymentStatus.replaceAll("_", " ")}</td><td className="px-5 py-4 capitalize">{(order.fulfillmentStatus === "unfulfilled" ? "pending" : order.fulfillmentStatus).replaceAll("_", " ")}</td><td className="px-5 py-4 text-muted-foreground">{date(order.createdAt)}</td></tr>)}</tbody></table></div>{customer.orders.length === 0 ? <p className="p-10 text-center text-sm text-muted-foreground">This customer has no orders yet.</p> : null}</section><aside className="space-y-5"><section className="rounded-xl border bg-background p-5"><h2 className="font-semibold">Contact</h2><div className="mt-3 space-y-1 text-sm text-muted-foreground"><p>{customer.email}</p><p>{customer.phone ?? "No phone provided"}</p></div></section><section className="rounded-xl border bg-background p-5"><h2 className="font-semibold">Summary</h2><dl className="mt-4 grid gap-4 text-sm"><div><dt className="text-muted-foreground">Orders</dt><dd className="mt-1 text-xl font-semibold">{customer.orderCount}</dd></div><div><dt className="text-muted-foreground">Total spent</dt><dd className="mt-1 text-xl font-semibold">{money(customer.totalSpentPaise)}</dd></div><div><dt className="text-muted-foreground">Last order</dt><dd className="mt-1">{customer.lastOrderAt ? date(customer.lastOrderAt) : "—"}</dd></div></dl></section></aside></div></>; }
