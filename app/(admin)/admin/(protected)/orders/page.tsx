import { AlertCircle, ClipboardList } from "lucide-react";
import { AdminOrdersTable } from "@/components/admin/admin-orders-table";
import { getAdminOrders } from "@/services/admin-orders";

export default async function AdminOrdersPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const params = await searchParams; const get = (key: string) => typeof params[key] === "string" ? params[key] : ""; const orders = await getAdminOrders({ q: get("q").slice(0, 100), payment: get("payment"), status: get("status"), sort: get("sort"), page: Number(get("page")) || 1 }); return <main className="mx-auto max-w-7xl p-5 sm:p-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-muted-foreground">Operations</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Orders</h1><p className="mt-2 text-sm text-muted-foreground">Review payment, fulfillment, and delivery status.</p></div><ClipboardList className="size-8 text-muted-foreground" /></div>{orders.error ? <div className="mt-6 flex gap-3 rounded-lg border border-destructive/30 bg-destructive/5 p-4 text-sm text-destructive"><AlertCircle className="size-5 shrink-0" />{orders.error}</div> : <AdminOrdersTable filters={{ q: get("q"), payment: get("payment"), status: get("status"), sort: get("sort") }} orders={orders.data} />}</main>; }
