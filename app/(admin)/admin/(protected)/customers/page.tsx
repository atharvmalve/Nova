import { AlertCircle, Users } from "lucide-react";
import { CustomerTable } from "@/components/admin/customer-table";
import { getAdminCustomers } from "@/services/admin-customers";
export default async function AdminCustomersPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const params = await searchParams; const q = typeof params.q === "string" ? params.q.slice(0, 100) : ""; const result = await getAdminCustomers({ q, page: Number(typeof params.page === "string" ? params.page : "") || 1 }); return <main className="mx-auto max-w-7xl p-5 sm:p-8"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-muted-foreground">Customers</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Customer history</h1><p className="mt-2 text-sm text-muted-foreground">Order-derived customer information for support and fulfillment.</p></div><Users className="size-8 text-muted-foreground" /></div>{result.error ? <div className="mt-6 flex gap-3 rounded-lg border border-destructive/30 bg-destructive/5 p-4 text-sm text-destructive"><AlertCircle className="size-5 shrink-0" />{result.error}</div> : <CustomerTable customers={result.data} q={q} />}</main>; }
